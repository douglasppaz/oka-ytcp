{
  "name": "oka-ytcp-server",
  "version": "0.0.0",
  "description": "OKa Youtube Cached Player - Server",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch src -e js --exec babel-node ./src/index.js",
    "test": "mocha --compilers js:babel-core/register --timeout 20000 -c tests/**/*.test.js",
    "test:watch": "mocha --compilers js:babel-core/register --timeout 20000 -c -w tests/**/*.test.js"
  },
  "author": "Douglas Paz",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.2",
    "fs-extra": "^2.0.0",
    "lodash": "^4.17.4",
    "youtube-dl": "^1.11.1"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-env": "^1.2.1",
    "mocha": "^3.2.0",
    "nodemon": "^1.11.0"
  }
}
